$if(domainSCIM)$
<b>SCIM</b> (System for Cross-domain Identity Management) is a standardised and open protocol which permits clouds and services that implement it to populate this domain with users.
$endif$